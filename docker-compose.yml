services:
  api:
    build: ./api
    volumes:
      - ./data:/app/data
    # 필요시 헬스체크 추가
    # expose:
    #   - "5000"

  web:
    image: nginx:1.25
    # api가 준비된 뒤 시작하도록 depends_on/healthcheck 구성 가능
    ports:
      - "8080:8080"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro